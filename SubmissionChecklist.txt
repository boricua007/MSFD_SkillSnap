Submission Checklist (Part 1)
    Solution with API and Client projects added ✔️
        MSFD_SkillSnap.Api and MSFD_SkillSnap.Client projects created and
        added to MSFD_SkillSnap.slnx.

    Models for PortfolioUser, Project, and Skill created ✔️
        PortfolioUser, Project, and Skill classes defined in
        MSFD_SkillSnap.Api/Models with a one-to-many relationship
        (PortfolioUser -> Projects, PortfolioUser -> Skills).

    Database successfully created with EF Core ✔️
        SkillSnapContext registered with SQLite, InitialCreate migration
        generated and applied.

    Static layout components scaffolded in Blazor ✔️
        ProfileCard, ProjectList, and SkillTags components created under
        MSFD_SkillSnap.Client/Components and rendered on the Home page.

    Copilot used to support and improve implementation ✔️
        Copilot used to refine ProfileCard into a parameterized component,
        configure EF Core relationships via Fluent API, and clean up
        Program.cs service registrations.

    Project saved for continuation in Part 2

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Submission Checklist (Part 2)
    API controllers for Projects and Skills added ✔️
        ProjectsController and SkillsController expose GET and POST endpoints.

    CORS configured to allow client communication ✔️
        The API allows requests from the HTTP and HTTPS Blazor client origins.

    Services in Blazor created and registered ✔️
        ProjectService and SkillService are registered and call the API endpoints.

    UI components connected to data sources ✔️
        ProjectList and SkillTags load their data through the registered services.

    Copilot used to generate, test, or refine logic ✔️
        Copilot was used to add Swagger, API request handling, error states, and
        project creation request validation.

    Data loaded and displayed in Blazor interface ✔️
        Projects and skills are loaded from the API and displayed in the client.

    Swagger/OpenAPI configured for API testing ✔️
        Swagger UI is available at http://localhost:5000/swagger in Development.

    API POST requests tested successfully ✔️
        Project creation was tested through Swagger using ProjectCreateRequest.

    Failed HTTP calls handled in Blazor ✔️
        ProjectList and SkillTags display loading, empty, and API error states.

    Runtime logs organized ✔️
        API and client runtime logs are stored in the ignored Logs/ folder.
